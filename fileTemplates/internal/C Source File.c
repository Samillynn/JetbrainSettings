#[[#include <bits/stdc++.h>

#ifdef MY_DEBUG
#define debug(x...) {cerr << "[" << #x << "] = ["; _print(x);}
#define dbg(x...) {cerr << "[" << #x << "] = "; __dbg(x);}
#else
#define debug(x...)
#define dbg(x...)
#endif

#define rep(i, n) for(int i = 0; i < (int)(n); ++i)
#define rep1(i, n) for(int i = 1; i <= (int)(n); ++i)
#define rvs(i, n) for(int i = (int)(n) - 1; i >= 0; --i)
#define rvs1(i, n) for(int i = (int)(n); i >= 1; --i)
#define init(a, n) memset((a), 0, (n) * (sizeof (a)[0]))

using namespace std;
typedef long long ll;
//#define int ll
typedef pair<int, int> pii;
typedef vector<int> vi;
typedef vector<vi> vvi;
const char ret = '\n';

const int M = 1e9 + 7;
const int N = 2e5 + 2;

void solve() {

}

int32_t main() {
        ios_base::sync_with_stdio(0);
        cin.tie(nullptr);
        int T = 1;
        cin >> T;
        while (T--) solve();
        return 0;
}]]#
